# Address printed by the Hardhat deployment script
REACT_APP_CONTRACT_ADDRESS=0xYourDeployedContractAddress

# Public URL of a server-side upload endpoint that accepts multipart form data
# and returns JSON in the form: { "IpfsHash": "..." }
REACT_APP_UPLOAD_ENDPOINT=http://localhost:3001/api/upload
